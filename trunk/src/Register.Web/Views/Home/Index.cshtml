@model Register.Web.Models.HomePageModel
@{
    ViewBag.Title = "หน้าหลัก";
}

<div ng-app="registerApp" ng-controller="registerIndexController">

    <div class="row justify-content-center">
        <div class="col-12 col-md-12 text-center">
            @Html.Partial("_Carousel")
        </div>
    </div>
    <div class="row justify-content-center mx-1 my-2 p-1">
        <div class="col-12 m-0 p-0 news-container" id="news-container" ng-include="resolveNewUrl()">

        </div>
    </div>
    @*<div class="row justify-content-center mx-1 my-2 p-1">
            <div class="col-12 p-0">
                @Html.Partial("_Highlights")
            </div>
        </div>

        <div class="row justify-content-center mx-1 my-2 p-1 border rounded border-site-primary">
            <div class="col-12">
                @Html.Partial("_News")
            </div>
        </div>*@
    <div class="modal fade" id="warningModal" tabindex="-1" role="dialog" aria-labelledby="warningModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg" role="document">
            <div class="modal-content">
                <div class="modal-header header-table">
                    <h5 class="modal-title font-weight-bold" id="warningModalLabel">ผู้สมัครโปรดทราบ</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body p-md-5 information text-center">
                    ผู้สมัครสามารถพิมพ์บัตรประจำตัวสอบได้ที่เมนู<br />"ค้นหาสถานที่สอบ และพิมพ์บัตรประจำตัวสอบ"
                </div>
                <div class="modal-footer justify-content-center ">
                    <button type="button" class="btn btn-success" data-dismiss="modal">รับทราบ</button>
                </div>
            </div>
        </div>
    </div>
</div>

@Html.Partial("_BrowserSupport")

@section scripts
{
    <script src="~/Scripts/views/register-index.js"></script>
    <script>
        var DIALOG_URL = "@Url.Action("RegisterStepDialog")";

        //if (TEST_TYPE_ID == "2") {
        //    $('#warningModal').modal({
        //        keyboard: false,
        //        backdrop: 'static'
        //    });
        //    $('#warningModal').modal('show');
        //}
    </script>
}
